# FTP download client

Client developed as part of Project 2 in RCOM course at FEUP

## Usage
To run the client you can use the Makefile for compilation
```sh
make
```

Afer this, run the binary:
```sh
./bin/download ftp://[<user>:<password>@]<host>/<url-path>
```
